version: '3.8'

services:
  # ==================== DATABASES ====================
  postgres:
    build:
      context: ./database
      dockerfile: Dockerfile
    container_name: jobportal-postgres
    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${POSTGRES_PORT}:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - jobportal-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"]
      interval: 10s
      timeout: 5s
      retries: 5

  mongo:
    image: mongo:7
    container_name: jobportal-mongo
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGO_USER}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGO_PASSWORD}
    ports:
      - "${MONGO_PORT}:27017"
    volumes:
      - mongo_data:/data/db
    networks:
      - jobportal-network

  # ==================== BACKEND SERVICES ====================
  springboot-service:
    build:
      context: ./springboot-service
      dockerfile: Dockerfile
    container_name: jobportal-springboot
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}
      SPRING_DATASOURCE_USERNAME: ${POSTGRES_USER}
      SPRING_DATASOURCE_PASSWORD: ${POSTGRES_PASSWORD}
    ports:
      - "${SPRING_BOOT_PORT}:8080"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - jobportal-network
    restart: on-failure

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: jobportal-nodejs
    environment:
      MONGO_URI: mongodb://${MONGO_USER}:${MONGO_PASSWORD}@mongo:27017/jobportal?authSource=admin
      PORT: 5000
    ports:
      - "${NODEJS_PORT}:5000"
    depends_on:
      - mongo
    networks:
      - jobportal-network
    restart: on-failure

  # ==================== API GATEWAY ====================
  api-gateway:
    build:
      context: ./api-gateway
      dockerfile: Dockerfile
    container_name: jobportal-gateway
    environment:
      SPRINGBOOT_URL: http://springboot-service:8080
      NODEJS_URL: http://server:5000
      GATEWAY_PORT: 8000
    ports:
      - "${GATEWAY_PORT}:8000"
    depends_on:
      - springboot-service
      - server
    networks:
      - jobportal-network
    restart: on-failure

  # ==================== FRONTEND ====================
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: jobportal-frontend
    ports:
      - "${FRONTEND_PORT}:80"
    depends_on:
      - api-gateway
    networks:
      - jobportal-network

volumes:
  postgres_data:
  mongo_data:


networks:
  jobportal-network:
    driver: bridge
